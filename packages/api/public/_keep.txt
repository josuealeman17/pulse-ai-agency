Placeholder so Vercel has a static output directory. All routes are served by the
serverless function in /api (see vercel.json). The function is bundled by Vercel's
esbuild-based builder, so no tsc build step is needed for deployment.
